# Copy to .env for local development (`netlify dev` loads it automatically).
# In production, set these in the Netlify dashboard: Site settings > Environment variables.
# The token stays server-side only — it is never exposed to the browser bundle.
DIRECTUS_URL=https://burnes-center.directus.app
DIRECTUS_TOKEN=your-token-here
